
#include<stdio.h>

int main() {

   int radius;
   float PI = 3.14, area, ci;

   printf("\nEnter radius of circle: ");
   scanf("%d", &radius);

   area = PI * radius * radius;
   printf("\nArea of circle : %f ", area);

   ci = 2 * PI * radius;
   printf("\nCircumference : %f ", ci);

   return (0);
